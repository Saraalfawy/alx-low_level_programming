#include "main.h"
/**
 *  main - entry point
 *  description: 'prints _putchar to stdout'
 *  Return: Always 0
 */
int main(void)
{
_char str[] = "_putchar";
int ch;
for (ch = 0; ch < 8; ch++)
_putchar(str[ch]);
_putchar('\n')

return (0);
}
